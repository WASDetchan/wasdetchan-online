version: "2"
sql:
  - engine: "postgresql"
    queries: "sql/query.sql"
    schema: "repository/migrations"
    gen:
      go:
        package: "repository"
        out: "repository"
        sql_package: "pgx/v5"
